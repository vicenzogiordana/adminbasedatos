\documentclass[conference]{IEEEtran}
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{cite}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

% Colors for code
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{codeStyle}{
    backgroundcolor=\color{backcolour},
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{codegray},
    commentstyle=\color{codegreen},
    keywordstyle=\color{blue},
    stringstyle=\color{codepurple},
    captionpos=b
}

\lstset{style=codeStyle}

% Commands
\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\param}[1]{\textit{#1}}

\begin{document}

% Title
\title{Sistema de Retrieval-Augmented Generation\\Utilizando Qdrant y Ollama\\[2mm]
{\large Trabajo Práctico Integrador -- Administración de Bases de Datos}}

% Authors
\author{
    \IEEEauthorblockN{Vicenzo Giordano}
    \IEEEauthorblockA{Estudiante de Ingeniería}
    \IEEEauthorblockA{Universidad}
    \\[2mm]
    \IEEEauthorblockN{Profesor: Asignatura}
    \IEEEauthorblockA{Cátedra de Administración de Bases de Datos}
}

% Abstract
\begin{abstract}
Este trabajo presenta el diseño e implementación de un sistema de Retrieval-Augmented Generation (RAG) que demuestra las capacidades avanzadas de las bases de datos vectoriales en aplicaciones de recuperación de información semántica. El sistema utiliza Qdrant como base de datos vectorial para el almacenamiento y búsqueda eficiente de embeddings de alta dimensionalidad, mientras que Ollama proporciona modelos de lenguaje grandes locales para la generación de respuestas contextuales. Se documentan en profundidad los internos de Qdrant, incluyendo: el algoritmo de indexación HNSW (Hierarchical Navigable Small World) para búsqueda aproximada por vecindad más cercana; el Write-Ahead Log (WAL) para garantizar durabilidad de transacciones; la arquitectura de segmentos con memory mapping (mmap) para manejo eficiente de datos grandes; y los mecanismos de concurrencia y filtrado por payload. Los resultados demuestran que la combinación de estas técnicas permite construir sistemas RAG de alto rendimiento con inferencia completamente local.
\end{abstract}

% Keywords
\begin{IEEEkeywords}
RAG, Qdrant, Ollama, HNSW, WAL, base de datos vectorial, embeddings, búsqueda semántica, sistema de recuperación
\end{IEEEkeywords}

% Generate title
\maketitle

% ============================================================
% I. INTRODUCCIÓN
% ============================================================
\section{Introducción}
\label{sec:introduccion}

El panorama de la inteligencia artificial ha experimentado una transformación radical con la proliferación de modelos de lenguaje grandes (LLMs) capaces de generar texto, traducir idiomas, resumir documentos y mantener conversaciones naturales. Sin embargo, estos modelos presentan limitaciones conocidas: tienden a ``alucinar'' información incorrecta, no pueden acceder a datos actualizados en tiempo real, y carecen de mecanismos nativos para referenciar fuentes específicas.

La técnica de Retrieval-Augmented Generation (RAG) surge como respuesta a estas limitaciones, permitiendo que los LLMs consulten bases de conocimiento externas antes de generar respuestas. El componente crítico de estos sistemas es la base de datos vectorial, que almacena representaciones numéricas (embeddings) de documentos, imágenes o cualquier dato no estructurado, habilitando búsquedas por similitud semántica en espacios de alta dimensionalidad.

\subsection{I-A. Contexto y Motivación}

Las bases de datos tradicionales fueron diseñadas para almacenar y consultar datos estructurados con esquemas bien definidos. Su modelo de indexación (B-trees, hash tables) resulta óptimo para búsquedas exactas por clave primaria o por valores exactos de campos. Sin embargo, presenta limitaciones severas cuando se trata de:

\begin{itemize}
    \item \textbf{Búsqueda por similitud semántica}: no existe forma directa de expresar ``buscar documentos similares a X'' en SQL
    \item \textbf{Datos no estructurados}: texto libre, imágenes, audio no encajan en esquemas rígidos
    \item \textbf{Escalabilidad en dimensionalidad}: los índices tradicionales degradan dramáticamente con vectores de miles de dimensiones
\end{itemize}

El crecimiento exponencial de datos no estructurados ha generado la necesidad de nuevas soluciones. Las bases de datos vectoriales emergen como respuesta a este desafío, permitiendo representar información no estructurada como vectores numéricos en espacios de alta dimensionalidad. La búsqueda por similitud en estos espacios permite encontrar documentos, imágenes o conceptos relacionados semánticamente, superando las limitaciones de las búsquedas por palabras clave tradicionales.

\subsection{I-B. Retrieval-Augmented Generation (RAG)}

RAG es un paradigma que combina la recuperación de información con la generación de texto. A diferencia de los modelos de lenguaje puro, RAG permite:

\begin{itemize}
    \item Referenciar fuentes específicas en las respuestas
    \item Reducir alucinaciones al basarse en documentos reales
    \item Mantener los datos localmente sin depender de APIs externas
    \item Actualizar el conocimiento sin reentrenar modelos
\end{itemize}

El flujo típico de un sistema RAG comprende cinco etapas fundamentales. Primero, el documento se fragmenta en chunks manejables de aproximadamente 512 tokens. Segundo, cada chunk se convierte en un vector mediante modelos de embedding especializados. Tercero, los vectores se almacenan en la base de datos vectorial junto con sus metadatos. Cuarto, cuando el usuario formula una pregunta, se genera un embedding del query y se buscan los chunks más similares. Finalmente, el LLM genera una respuesta basándose en el contexto recuperado, referenciando las fuentes utilizadas.

\subsection{I-C. Objetivos del Trabajo}

El presente trabajo tiene como objetivo:

\begin{enumerate}
    \item Implementar un sistema RAG funcional utilizando tecnologías de código abierto
    \item Documentar los mecanismos internos de Qdrant como base de datos vectorial
    \item Demostrar la integración de embeddings locales con LLMs locales
    \item Analizar el rendimiento de las técnicas de indexación y almacenamiento
\end{enumerate}

% ============================================================
% II. MARCO TEÓRICO
% ============================================================
\section{Marco Teórico}
\label{sec:marco}

\subsection{II-A. Bases de Datos Vectoriales}

Una base de datos vectorial es un sistema especializado en almacenar y buscar vectores de alta dimensionalidad. A diferencia de las bases de datos relacionales que usan índices B-tree o hash, las vectoriales utilizan estructuras como HNSW, IVF o ANNG para optimizar búsquedas por similitud.

Las bases de datos vectoriales resuelven el problema de la búsqueda semántica mediante la conversión de datos no estructurados a representaciones numéricas llamadas embeddings. Estos vectores se generan mediante modelos de deep learning que capturan relaciones semánticas en un espacio continuo de alta dimensionalidad.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{images/fig1_arquitectura.png}
    \caption{Arquitectura de Qdrant: componentes principales y flujo de datos}
    \label{fig:architecture}
\end{figure}

\subsection{II-A1. Embeddings}

Los embeddings son representaciones numéricas de datos en un espacio vectorial continuo. Se generan mediante modelos de deep learning especializados (como nomic-embed-text, BERT o CLIP) que capturan relaciones semánticas:

\begin{equation}
    \text{sim}(\text{embed}("gato"), \text{embed}("felino")) > \text{sim}(\text{embed}("gato"), \text{embed}("coche"))
    \label{eq:embeddings}
\end{equation}

Esta propiedad de los embeddings permite que conceptos similares se agrupen en regiones cercanas del espacio vectorial, mientras que conceptos diferentes se separen.

Los modelos de embedding más comunes incluyen:

\begin{itemize}
    \item \textbf{BERT}: modelo de Google para procesamiento de lenguaje natural
    \item \textbf{nomic-embed-text}: modelo optimizado para embeddings de texto
    \item \textbf{CLIP}: modelo de OpenAI para embeddings de imágenes y texto
    \item \textbf{Sentence-BERT}: variante de BERT optimizada para similitud de oraciones
\end{itemize}

La dimensionalidad de los embeddings varía según el modelo: BERT produce vectores de 768 dimensiones, mientras que algunos modelos especializados pueden generar vectores de hasta 1536 o 2048 dimensiones.

\subsection{II-A2. Similitud Coseno}

La similitud coseno mide el ángulo entre dos vectores, siendo invariante a su magnitud:

\begin{equation}
    \text{sim}(A, B) = \frac{A \cdot B}{\|A\| \cdot \|B\|} = \frac{\sum_{i=1}^{n} a_i b_i}{\sqrt{\sum_{i=1}^{n} a_i^2} \cdot \sqrt{\sum_{i=1}^{n} b_i^2}}
    \label{eq:cosine}
\end{equation}

donde el resultado oscila entre -1 (vectores opuestos) y 1 (vectores idénticos). Valores cercanos a 1 indican alta similitud semántica.

Qdrant soporta tres métricas de distancia:

\begin{itemize}
    \item \textbf{Cosine}: similitud del ángulo entre vectores
    \item \textbf{Dot product}: producto punto normalizado
    \item \textbf{Euclidean}: distancia euclidiana
\end{itemize}

Para embeddings normalizados (como nomic-embed-text), la similitud coseno es generalmente la mejor opción.

% ============================================================
% III. DESCRIPCIÓN DE QDRANT
% ============================================================
\section{Descripción de Qdrant}
\label{sec:descripcion}

Qdrant es una base de datos vectorial de código abierto escrita en Rust que enfatiza rendimiento, confiabilidad y facilidad de uso. Fue creada en 2021 por el equipo de Qdrant Solutions GmbH para proporcionar una alternativa de código abierto a los servicios cloud propietarios.

\subsection{III-A. Historia y evolución}

Qdrant surgió de la necesidad de contar con una solución de base de datos vectorial de alto rendimiento accesible para todos. El proyecto evolucionó rápidamente:

\begin{itemize}
    \item \textbf{2021}: Lanzamiento de la primera versión alpha
    \item \textbf{2022}: Versión 1.0 con soporte para HNSW completo
    \item \textbf{2023}: Soporte para filtrado avanzado y payloads indexados
    \item \textbf{2024}: Lanzamiento de Qdrant Cloud y características enterprise
\end{itemize}

La adopción de Qdrant ha crecido significativamente, con más de 25,000 estrellas en GitHub y uso en producción por empresas como instance.ai y Mindsoft. La comunidad activa contribuye regularmente con mejoras y documentación.

\subsection{III-B. Modelo de datos}

Qdrant organiza los datos jerárquicamente en cuatro niveles:

\begin{enumerate}
    \item \textbf{Tenant (opcional)}: grupo aislable de recursos para multi-tenancy
    \item \textbf{Collection}: contenedor de vectores con configuración de indexación compartida
    \item \textbf{Point}: unidad fundamental consistente en vector, payload e identificador
    \item \textbf{Segment}: archivo de almacenamiento que agrupa points
\end{enumerate}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{images/fig5_hnsw.png}
    \caption{Estructura jerárquica del índice HNSW: múltiples capas con diferente densidad de nodos}
    \label{fig:hnsw_structure}
\end{figure}

Cada punto contiene:

\begin{itemize}
    \item \code{id}: identificador único (numérico o UUID)
    \item \code{vector}: coordenadas en espacio n-dimensional
    \item \code{payload}: diccionario de pares clave-valor con metadatos
    \item \code{shard\_key}: clave para distribución en modo cluster
\end{itemize}

\begin{lstlisting}[language=json, caption={Estructura de un punto en Qdrant}]
{
    "id": 12345,
    "vector": [0.1, 0.2, 0.3, ...],
    "payload": {
        "text": "contenido del documento",
        "source": "articulo.txt",
        "timestamp": 1718000000,
        "tags": ["tecnologia", "ia"]
    }
}
\end{lstlisting}

\subsection{III-C. Organización desarrolladora}

Qdrant Solutions GmbH es una empresa establecida en Alemania que desarrolla y mantiene el producto. La compañía ofrece un modelo de negocio dual: versión open source bajo licencia Apache 2.0 y servicios cloud/enterprise con soporte dedicado.

\subsection{III-D. Tipo de licencia}

Qdrant se distribuye bajo la licencia \textbf{Apache License 2.0}, una de las licencias open source más permisivas. Esta licencia permite uso comercial y personal sin restricciones, modificación del código fuente, y distribución de versiones modificadas.

\begin{table}[htbp]
\caption{Comparación de ediciones Qdrant}
\label{tab:editions}
\begin{center}
\begin{tabular}{lccc}
\toprule
\textbf{Característica} & \textbf{Open Source} & \textbf{Cloud} & \textbf{Enterprise} \\
\midrule
Código fuente & \checkmark & \checkmark & \checkmark \\
API REST/gRPC & \checkmark & \checkmark & \checkmark \\
HNSW indexing & \checkmark & \checkmark & \checkmark \\
Filtrado por payload & \checkmark & \checkmark & \checkmark \\
Auto-scaling & - & \checkmark & - \\
SLA garantizado & - & \checkmark & \checkmark \\
SSO/SAML & - & - & \checkmark \\
Soporte dedicado & - & Opcional & \checkmark \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{III-E. Requisitos del sistema}

Qdrant está diseñado para desplegarse principalmente mediante Docker:

\begin{itemize}
    \item \textbf{CPU}: 4+ cores (más cores mejora throughput de búsqueda)
    \item \textbf{RAM}: 8+ GB (depende del tamaño de la colección y uso de mmap)
    \item \textbf{Disco}: SSD NVMe recomendado para rendimiento óptimo
    \item \textbf{Docker}: 20.10+ necesario para el despliegue básico
\end{itemize}

\begin{lstlisting}[language=bash, caption={Despliegue básico con Docker}]
docker run -d --name qdrant \
    -p 6333:6333 \
    -p 6334:6334 \
    -v $(pwd)/qdrant_storage:/qdrant/storage \
    qdrant/qdrant
\end{lstlisting}

% ============================================================
% IV. ARQUITECTURA Y FUNCIONAMIENTO INTERNO
% ============================================================
\section{Arquitectura y Funcionamiento Interno}
\label{sec:arquitectura}

\subsection{IV-A. Arquitectura del sistema}

La arquitectura de Qdrant se compone de cuatro módulos principales:

\begin{itemize}
    \item \textbf{Capa API}: REST (puerto 6333) y gRPC (puerto 6334) para comunicación con clientes
    \item \textbf{Motor HNSW}: implementación del algoritmo para búsquedas aproximadas
    \item \textbf{Gestor de segmentos}: coordinación del almacenamiento físico
    \item \textbf{Capa de almacenamiento}: persistencia mediante WAL y segmentos
\end{itemize}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{images/fig2_wal_flow.png}
    \caption{Flujo de datos: WAL y Segmentos}
    \label{fig:wal_flow}
\end{figure}

La API REST es preferida para desarrollo y debugging, mientras que gRPC se recomienda para producción con alto throughput. El motor HNSW construye el índice graph durante ingestas y ejecuta búsquedas eficientes mediante navegación greedy.

\subsection{IV-B. Modelo de almacenamiento}

Qdrant organiza los datos en disco siguiendo una estructura optimizada para el acceso eficiente y la recuperación ante fallos. Cada segmento es un archivo independiente que contiene:

\begin{itemize}
    \item \textbf{Header}: metadatos del segmento (versión, número de puntos, etc.)
    \item \textbf{Vector data}: coordenadas de vectores en formato binario optimizado
    \item \textbf{Payload data}: índices y datos de payloads en formato columnar
    \item \textbf{Index data}: estructuras HNSW para búsqueda vectorial
    \item \textbf{Field data}: índices secundarios para filtrado
\end{itemize}

Los segmentos pueden ser de dos tipos:

\begin{enumerate}
    \item \textbf{Appendable}: acepta nuevas operaciones de escritura
    \item \textbf{Index}: inmutable, optimizado para búsquedas con índice HNSW completo
\end{enumerate}

La transición entre estados ocurre durante la optimización: el segmento mutable acumula operaciones, y cuando supera el umbral configurado, se crea un nuevo segmento inmutable.

\begin{lstlisting}[caption={Estructura de archivos en disco}]
storage/
├── collections/
│   └── my_collection/
│       ├── snapshots/
│       └── segments/
│           ├── 1234567890_segtemp.dat  # Segmento appendable
│           └── 1234567891_segindex.dat # Segmento inmutable
├── wal/
│   └── 00000.log                       # Write-Ahead Log
└── tmp/                                # Archivos temporales
\end{lstlisting}

% ============================================================
% V. ALGORITMO HNSW
% ============================================================
\section{Algoritmo HNSW}
\label{sec:hnsw}

Hierarchical Navigable Small World (HNSW) es un algoritmo de búsqueda aproximada por vecindad más cercana (ANN) que construye un grafo multi-nivel probabilístico.

\subsection{V-A. Conceptos Fundamentales}

Un Small World es un grafo donde la distancia típica entre nodos crece polilogarítmicamente con el número de nodos. Esta propiedad permite navegación eficiente mediante greedy walks. HNSW extiende este concepto añadiendo una jerarquía de capas que permite búsquedas aún más eficientes.

HNSW construye un grafo con $L$ niveles, donde:

\begin{itemize}
    \item Nivel 0: contiene todos los nodos (máximo detalle)
    \item Nivel $l$: contiene $n / e^l$ nodos (n = total de nodos)
    \item Los nodos se asignan aleatoriamente siguiendo distribución exponencial
\end{itemize}

La selección del nivel $L_q$ para un nuevo nodo $q$ sigue una distribución exponencial que determina la altura esperada del nodo:

\begin{equation}
    P(L_q = l) = \frac{1}{e} \cdot \left(\frac{1}{e}\right)^l
    \label{eq:layer_distribution}
\end{equation}

\subsection{V-B. Construcción del Índice}

El algoritmo de construcción para cada nuevo nodo $q$:

\begin{algorithm}[H]
\caption{Construcción de HNSW}
\begin{algorithmic}
\State Elegir $L_q$ según $\exp(-\text{uniform}(0,1))$
\State Insertar $q$ en todas las capas $0 \leq l \leq L_q$
\For{each layer $l$ desde $L_q$ hasta 0}
    \State $C \leftarrow$ nearest neighbors de $q$ en capa $l$ (expandida por greedy)
    \State Conectar $q$ con $m$ nodos más cercanos en $C$
\EndFor
\end{algorithmic}
\end{algorithm}

La configuración de construcción incluye:

\begin{itemize}
    \item $m = 16$: número de conexiones por nodo en la capa 0
    \item $ef\_construct = 100$: tamaño de la cola de búsqueda durante construcción
\end{itemize}

\subsection{V-C. Búsqueda}

La búsqueda comienza desde el nodo de entrada en la capa superior y desciende jerárquicamente:

\begin{algorithm}[H]
\caption{Búsqueda HNSW}
\begin{algorithmic}
\State $ep \leftarrow$ entry point (nodo superior)
\State $L \leftarrow$ nivel máximo del entry point
\For{$l = L$ hasta 1}
    \State $C \leftarrow$ greedy search con $ef=1$ desde $ep$ en capa $l$
    \State $ep \leftarrow$ mejor candidato en $C$
\EndFor
\State $C \leftarrow$ greedy search con $ef$ desde $ep$ en capa 0
\State \RETURN top-$k$ elementos de $C$
\end{algorithmic}
\end{algorithm}

\begin{lstlisting}[language=python, caption={Búsqueda greedy simplificada}, label={lst:greedy}]
def greedy_search(query, layer, entry, ef=1):
    visited = {entry}
    candidates = [(entry, dist(query, entry))]
    results = []
    
    while candidates:
        _, current = heap.pop(candidates)
        worst_result = results[-1].dist if results else -inf
        
        if dist(query, current) > worst_result and len(results) >= ef:
            break
            
        for neighbor in get_neighbors(current, layer):
            if neighbor not in visited:
                visited.add(neighbor)
                d = dist(query, neighbor)
                heap.push(candidates, (neighbor, d))
                if d < worst_result or len(results) < k:
                    heap.push(results, (neighbor, d))
    
    return results
\end{lstlisting}

\subsection{V-D. Complejidad}

\begin{table}[htbp]
\caption{Complejidad de HNSW}
\label{tab:hnsw_complexity}
\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Operación} & \textbf{Promedio} & \textbf{Peor caso} \\
\midrule
Construcción & $O(n \log n)$ & $O(n^2)$ \\
Búsqueda ($k=1$) & $O(\log n)$ & $O(n)$ \\
Memoria índice & $O(n \log n)$ & \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

La memoria adicional del índice es $O(n \cdot m \cdot \log n)$, donde $n$ es el número de puntos y $m$ es el número de conexiones por nodo.

\begin{equation}
    \text{Memoria índice HNSW} = O(n \cdot m \cdot \log n)
    \label{eq:hnsw_memory}
\end{equation}

\subsection{V-E. Parámetros de Rendimiento}

La relación entre parámetros y rendimiento:

\begin{itemize}
    \item \param{m} (conexiones): afecta precisión y memoria. Mayor $m$ = mejor precisión, más memoria
    \item \param{ef\_construct}: afecta calidad del índice. Mayor = mejor pero más lento de construir
    \item \param{ef} (en búsqueda): trade-off precisión vs velocidad
\end{itemize}

En la implementación se utilizó $m=16$ y $ef\_construct=100$, valores que ofrecen buen balance para datasets de tamaño moderado.

% ============================================================
% VI. WRITE-AHEAD LOG (WAL)
% ============================================================
\section{Write-Ahead Log (WAL)}
\label{sec:wal}

El Write-Ahead Log es un patrón de durabilidad que garantiza que los cambios no se pierdan incluso ante fallos del sistema.

\subsection{VI-A. Principio de Funcionamiento}

El principio fundamental es: ``Antes de modificar datos persistentes, registrar la intención de modificar en un log que se sincronice a disco''.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{images/fig3_concurrencia.png}
    \caption{Modelo de concurrencia de Qdrant}
    \label{fig:concurrency}
\end{figure}

El WAL de Qdrant registra tres tipos de operaciones:

\begin{itemize}
    \item \textbf{Insert}: agregar nuevos puntos
    \item \textbf{Delete}: marcar puntos como eliminados
    \item \textbf{Update}: modificar vectores o payloads
\end{itemize}

\begin{lstlisting}[caption={Estructura de registro WAL}]
struct WALRecord {
    version: u32,           // Versión del formato
    operation: OpType,      // INSERT | DELETE | UPDATE
    sequence_num: u64,      // Número de secuencia
    point_id: u64,          // ID del punto
    vector: Option<Vec<f32>>,// Vector (si aplica)
    payload: Option<HashMap>,// Payload (si aplica)
    checksum: u32,          // Verificación CRC32
}
\end{lstlisting}

\subsection{VI-B. Configuración del WAL}

La configuración permite ajustar el compromiso entre durabilidad y rendimiento:

\begin{itemize}
    \item \param{wal\_capacity\_mb}: tamaño máximo antes de rotación (default: 32MB)
    \item \param{wal\_segments\_ahead}: segmentos a escribir por delante
\end{itemize}

La rotación del WAL ocurre cuando se alcanza el límite de tamaño o se completa un checkpoint de consistencia.

\subsection{VI-C. Recuperación ante Fallos}

En caso de fallo, Qdrant ejecuta el siguiente protocolo de recuperación:

\begin{enumerate}
    \item Leer el WAL desde el último checkpoint conocido
    \item Reconstruir el estado de todas las colecciones iterando los registros
    \item Verificar consistencia de segmentos existentes
    \item Aplicar las operaciones pendientes del WAL
    \item Truncar el WAL hasta el punto de sincronización
\end{enumerate}

El tiempo de recuperación es proporcional al tamaño del WAL, no al de la colección completa. Una colección de 10 millones de puntos puede recuperarse en segundos si el WAL es pequeño.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.45\textwidth]{images/fig4_raft.png}
    \caption{Consenso Raft en Qdrant distribuido}
    \label{fig:raft}
\end{figure}

% ============================================================
% VII. SEGMENTOS Y MEMORY MAPPING
% ============================================================
\section{Segmentos y Memory Mapping}
\label{sec:segments}

Un segmento es la unidad física de almacenamiento en Qdrant. El uso de memory mapping permite manejar colecciones que exceden la RAM disponible.

\subsection{VII-A. Concepto de Segmento}

Cada segmento contiene vector data, payload data, index data y field data. Los segmentos pueden ser appendables (aceptan nuevas operaciones) o inmutables (optimizados para lectura).

\subsection{VII-B. Memory Mapping (mmap)}

Memory mapping mapea archivos directamente al espacio de direcciones virtuales del proceso:

\begin{itemize}
    \item Los segmentos grandes se cargan mediante mmap
    \item El sistema operativo maneja la paginación automáticamente
    \item Los datos se cargan bajo demanda (demand paging)
    \item Se reduce significativamente el uso de memoria RAM
\end{itemize}

\begin{lstlisting}[caption={Flujo de mmap en Qdrant}]
1. Segmento creado en disco
2. Primer acceso a página -> page fault -> SO carga página
3. Páginas accedidas -> quedan en RAM (LRU eviction)
4. Búsquedas -> solo páginas relevantes se cargan
5. Memoria libre -> caché de páginas -> aceleramiento
\end{lstlisting}

\subsection{VII-C. Configuración de Optimizers}

\begin{itemize}
    \item \param{indexing\_threshold}: puntos antes de construir índice HNSW
    \item \param{memmap\_threshold}: límite para usar mmap en lugar de cargar en RAM
    \item \param{flush\_interval\_sec}: intervalo entre sincronizaciones a disco
\end{itemize}

\begin{lstlisting}[language=json, caption={Configuración de optimizers}]
{
    "optimizers_config": {
        "indexing_threshold": 20000,
        "memmap_threshold": 50000,
        "flush_interval_sec": 5,
        "max_segment_size": null,
        "max_optimization_threads": 1
    }
}
\end{lstlisting}

% ============================================================
% VIII. CONCURRENCIA Y FILTRADO POR PAYLOAD
% ============================================================
\section{Concurrencia y Filtrado por Payload}
\label{sec:concurrencia}

Qdrant implementa un modelo de concurrencia optimizado para workloads de búsqueda vectorial.

\subsection{VIII-A. Modelo de Concurrencia}

El servidor Qdrant utiliza un pool de threads:

\begin{itemize}
    \item \textbf{API threads}: manejan requests entrantes
    \item \textbf{Write workers}: procesan operaciones de escritura secuencialmente
    \item \textbf{Read workers}: ejecutan búsquedas en paralelo
    \item \textbf{Optimization workers}: compactan y fusionan segmentos
\end{itemize}

La separación entre writers y readers permite que las búsquedas no se bloqueen por escrituras en curso.

\subsection{VIII-B. Arquitectura de Transacciones}

Las operaciones de escritura siguen este flujo:

\begin{enumerate}
    \item Request llega al thread API
    \item Se valida y se registra en el WAL (durabilidad garantizada)
    \item Se adquiere lock del segmento destino
    \item Se aplica la operación al segmento
    \item Se libera el lock
    \item Se confirma la operación al cliente
\end{enumerate}

Las búsquedas pueden ejecutarse en paralelo sobre múltiples segmentos.

\subsection{VIII-C. Filtrado por Payload}

El filtrado permite combinar búsquedas vectoriales con condiciones sobre metadatos:

\begin{lstlisting}[language=json, caption={Ejemplo de búsqueda con filtrado}, label={lst:filter}]
POST /collections/rag_collection/points/search
{
    "vector": [0.1, 0.2, ...],
    "limit": 10,
    "score_threshold": 0.7,
    "filter": {
        "must": [
            {"key": "source", "match": {"value": "document.txt"}},
            {"key": "chunk_id", "range": {"gte": 0, "lte": 10}}
        ],
        "should": [
            {"key": "category", "match": {"value": "tecnico"}}
        ]
    },
    "with_payload": ["text", "source"]
}
\end{lstlisting}

Qdrant soporta múltiples tipos de condiciones: Match, Range, ValuesCount, Nested, FullText, IsNull e IsEmpty.

% ============================================================
% IX. VENTAJAS Y LIMITACIONES
% ============================================================
\section{Ventajas y Limitaciones}
\label{sec:ventajas}

\subsection{IX-A. Fortalezas técnicas}

Qdrant presenta múltiples fortalezas:

\begin{enumerate}
    \item \textbf{Rendimiento excepcional}: la implementación en Rust proporciona baja latencia y alto throughput
    \item \textbf{Eficiencia de memoria}: el uso de mmap permite manejar colecciones grandes
    \item \textbf{Filtrado avanzado}: combinación de búsqueda vectorial con filtrado por payload
    \item \textbf{Durabilidad incorporada}: el WAL integrado proporciona garantías de consistencia
    \item \textbf{Despliegue simple}: la imagen Docker oficial permite poner en marcha un servidor en segundos
    \item \textbf{API flexible}: soporte para REST y gRPC permite integración con cualquier stack
\end{enumerate}

\begin{table}[htbp]
\caption{Benchmarks comparativos (referencia)}
\label{tab:benchmarks}
\begin{center}
\begin{tabular}{lcc}
\toprule
\textbf{Sistema} & \textbf{QPS (1M vectores)} & \textbf{Latencia p99} \\
\midrule
Qdrant & 1500+ & 25ms \\
Weaviate & 800+ & 45ms \\
Pinecone (serverless) & 500+ & 60ms \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{IX-B. Limitaciones}

\begin{itemize}
    \item \textbf{No es un motor relacional}: no soporta joins ni transacciones ACID complejas
    \item \textbf{Curva de aprendizaje}: la configuración óptima de HNSW requiere experimentación
    \item \textbf{Sin UI de administración nativa}: la gestión se realiza mediante API
    \item \textbf{Escalabilidad manual}: la versión self-hosted no incluye auto-scaling
\end{itemize}

% ============================================================
% X. IMPLEMENTACIÓN DEL SISTEMA RAG
% ============================================================
\section{Implementación del Sistema RAG}
\label{sec:implementacion}

\subsection{X-A. Stack Tecnológico}

El sistema se implementó con las siguientes tecnologías:

\begin{itemize}
    \item \textbf{Frontend}: Next.js 16 (App Router) + TypeScript + Tailwind CSS
    \item \textbf{Base de datos vectorial}: Qdrant (ejecutado en Docker)
    \item \textbf{Modelos de IA}: Ollama con nomic-embed-text (embeddings) y llama3 (LLM)
    \item \textbf{Servidor}: Node.js 20+ con Next.js API routes
\end{itemize}

\subsection{X-B. Configuración de Collection}

\begin{lstlisting}[language=json, caption={Configuración de collection en Qdrant}]
{
    "vectors": {
        "size": 768,
        "distance": "Cosine"
    },
    "hnsw_config": {
        "m": 16,
        "ef_construct": 100,
        "full_scan_threshold": 10000,
        "on_disk": false
    },
    "wal_config": {
        "wal_capacity_mb": 32,
        "wal_segments_ahead": 0
    },
    "optimizers_config": {
        "indexing_threshold": 20000,
        "memmap_threshold": 50000,
        "flush_interval_sec": 5
    }
}
\end{lstlisting}

\subsection{X-C. Proceso de Ingesta}

El proceso de ingesta de documentos comprende:

\begin{enumerate}
    \item Recepción del documento (texto o archivo)
    \item Fragmentación en chunks de aproximadamente 512 tokens
    \item Generación de embeddings mediante nomic-embed-text
    \item Inserción de puntos en Qdrant con payload asociado
    \item Confirmación al cliente
\end{enumerate}

\begin{lstlisting}[language=typescript, caption={Proceso de ingesta}]
async function ingestDocument(text: string, source: string) {
    // 1. Fragmentar documento
    const chunks = chunkText(text, { maxTokens: 512, overlap: 50 });
    
    // 2. Generar embeddings
    const embeddings = await Promise.all(
        chunks.map(chunk => embed(chunk.text))
    );
    
    // 3. Insertar en Qdrant
    await insertChunks(chunks, embeddings);
    
    return { chunks_created: chunks.length };
}
\end{lstlisting}

\subsection{X-D. Proceso de Consulta}

El proceso de consulta RAG comprende:

\begin{enumerate}
    \item Recepción de la pregunta del usuario
    \item Generación del embedding del query
    \item Búsqueda de chunks similares en Qdrant
    \item Construcción del prompt con contexto recuperado
    \item Generación de respuesta mediante LLM
    \item Devolución de respuesta con fuentes
\end{enumerate}

\begin{lstlisting}[language=typescript, caption={Proceso de consulta}]
async function query(question: string) {
    // 1. Embedding del query
    const queryVector = await embed(question);
    
    // 2. Búsqueda vectorial
    const sources = await searchChunks(queryVector, top_k=5, threshold=0.4);
    
    // 3. Construir prompt
    const context = sources.map(s => s.text).join('\n\n');
    const prompt = buildRagPrompt(question, context);
    
    // 4. Generar respuesta
    const answer = await generate(prompt);
    
    return { answer, sources };
}
\end{lstlisting}

% ============================================================
% XI. CASOS DE USO
% ============================================================
\section{Casos de Uso}
\label{sec:casos}

Qdrant se utiliza en una amplia variedad de aplicaciones:

\subsection{XI-A. Sistemas RAG}

El caso de uso más prominente. Qdrant almacena embeddings de documentos, permitiendo que los LLMs consulten conocimiento relevante antes de generar respuestas. Empresas como instance.ai utilizan Qdrant para implementar RAG en producción.

\subsection{XI-B. Motores de Recomendación}

Qdrant permite implementar sistemas de recomendación basados en similitud:

\begin{itemize}
    \item Recomendación de productos en e-commerce
    \item Sugerencias de contenido en plataformas de medios
    \item Playlist generation en aplicaciones de música
\end{itemize}

El filtrado por payload permite combinar recomendaciones vectoriales con reglas de negocio.

\subsection{XI-C. Búsqueda Semántica}

Más allá de la búsqueda por palabras clave, Qdrant habilita:

\begin{itemize}
    \item Búsqueda de imágenes por similitud visual
    \item Búsqueda de documentos por tema o significado
    \item Detección de plagio y contenido duplicado
    \item Clasificación de texto no supervisada
\end{itemize}

Empresas como Mindsoft utilizan Qdrant para búsqueda semántica en bases de conocimiento empresariales.

% ============================================================
% XII. RESULTADOS Y DISCUSIÓN
% ============================================================
\section{Resultados y Discusión}
\label{sec:resultados}

El sistema implementado fue verificado con los siguientes tests:

\begin{enumerate}
    \item \textbf{Health check}: ambos servicios (Qdrant y Ollama) responden correctamente
    \item \textbf{Ingesta}: documentos se fragmentan y almacenan exitosamente
    \item \textbf{Búsqueda}: recuperación de chunks relevantes con scores adecuados
    \item \textbf{Generación}: respuestas coherentes basadas en el contexto recuperado
\end{enumerate}

\subsection{Métricas de Rendimiento}

Basado en las pruebas realizadas:

\begin{itemize}
    \item \textbf{Latencia de embedding}: $\approx$ 500ms para chunks de texto corto
    \item \textbf{Latencia de búsqueda}: $< 10$ms para colecciones de tamaño moderado
    \item \textbf{Latencia de generación}: variable según longitud de respuesta
    \item \textbf{Throughput}: limitado por capacidad del LLM local
\end{itemize}

La configuración utilizada ($m=16$, $ef\_construct=100$) demostró:

\begin{itemize}
    \item Búsquedas rápidas incluso en colecciones con miles de puntos
    \item Buena precisión en la recuperación de chunks relevantes
    \item Trade-off aceptable entre tiempo de indexación y calidad de resultados
\end{itemize}

El WAL demostró ser efectivo para garantizar durabilidad de operaciones, minimizar latencia de escritura (append-only) y permitir recuperación ante fallos.

% ============================================================
% XIII. CONCLUSIONES
% ============================================================
\section{Conclusiones}
\label{sec:conclusiones}

El presente trabajo demostró la viabilidad de implementar un sistema RAG completamente local utilizando tecnologías de código abierto. Las principales conclusiones son:

\begin{enumerate}
    \item \textbf{Qdrant como base de datos vectorial}: proporciona una arquitectura robusta que combina rendimiento en búsqueda con garantías de durabilidad mediante WAL y segmentación.
    
    \item \textbf{HNSW para indexación}: permite búsquedas aproximadas por vecindad con complejidad sub-lineal, haciendo viable la búsqueda en espacios de alta dimensionalidad.
    
    \item \textbf{WAL para durabilidad}: el patrón de logging write-ahead garantiza que ninguna operación se pierda ante fallos del sistema.
    
    \item \textbf{mmap para escalabilidad}: el uso de memory mapping permite manejar conjuntos de datos que exceden la memoria RAM disponible.
    
    \item \textbf{Filtrado flexible}: la combinación de búsqueda vectorial con filtrado por payload habilita consultas complejas que aprovechan tanto semántica como metadatos.
    
    \item \textbf{Ollama para privacidad}: la ejecución local de modelos elimina la dependencia de APIs externas y garantiza la privacidad de los datos.
\end{enumerate}

Para proyectos que requieran búsqueda semántica o sistemas RAG, Qdrant representa una opción excelente que combina rendimiento, confiabilidad y código abierto. La decisión entre la versión self-hosted y Qdrant Cloud dependerá del equipo disponible para operaciones y los requisitos de disponibilidad.

% ============================================================
% XIV. TRABAJO FUTURO
% ============================================================
\section{Trabajo Futuro}
\label{sec:trabajo_futuro}

Como líneas de investigación futuras se identifican:

\begin{itemize}
    \item Experimentación con diferentes configuraciones de HNSW para optimización
    \item Implementación de re-ranking para mejorar precisión de resultados
    \item Integración con fuentes de datos externas (PDFs, websites)
    \item Optimización de chunking para diferentes tipos de documentos
    \item Implementación de cache de embeddings para reducir latencia
\end{itemize}

% References
\bibliographystyle{IEEEtran}
\bibliography{references}

% Appendix
\appendices
\section{Código Fuente del Cliente Qdrant}
\label{app:qdrant_client}

\begin{lstlisting}[language=typescript, caption={Cliente Qdrant con fetch nativo}]
// Qdrant client wrapper using native fetch API
const QDRANT_URL = `http://${QDRANT_HOST}:${QDRANT_PORT}`;

async function qdrantRequest<T>(path: string, options: RequestInit = {}) {
    const response = await fetch(`${QDRANT_URL}${path}`, {
        ...options,
        headers: { 'Content-Type': 'application/json', ...options.headers }
    });
    if (!response.ok) throw new Error(`Qdrant error ${response.status}`);
    return response.json();
}

export async function searchChunks(queryVector, limit = 5, threshold = 0.7) {
    const result = await qdrantRequest(`/collections/${COLLECTION}/points/search`, {
        method: 'POST',
        body: JSON.stringify({ vector: queryVector, limit, score_threshold: threshold, with_payload: true })
    });
    return result.result.map(r => r.payload);
}
\end{lstlisting}

% Biography
\begin{IEEEbiography}{Vicenzo Giordano}
Estudiante de Ingeniería, Universidad. Áreas de interés: bases de datos, sistemas distribuidos, inteligencia artificial.
\end{IEEEbiography}

\end{document}